(() => {
 const labels=[['responsive','Mobile and desktop verified'],['links','Links, forms, and buttons tested'],['content','Content and contact details verified'],['console','No console errors or broken assets']];let items=[];const root=document.getElementById('queue'),status=document.getElementById('status');const msg=(t,e=false)=>{status.textContent=t;status.classList.toggle('error',e)};
 async function load(){try{items=await SitePipeline.list(['review']);render()}catch(e){msg(e.message,true)}}function render(){root.innerHTML=items.map(x=>`<article class="review-grid card"><section class="review-panel"><div class="upload-head"><span class="badge">Under review</span><button class="btn secondary" data-download="${x.id}">Download All (.zip)</button></div><h2>${SitePipeline.escapeHTML(x.company)}</h2><div class="file-tree">${x.files.map(f=>`<span><i class="bi bi-file-earmark-code"></i><b>${SitePipeline.escapeHTML(f.path)}</b><small>${f.size} B</small></span>`).join('')}</div><textarea data-notes="${x.id}" placeholder="Review notes">${SitePipeline.escapeHTML(x.notes)}</textarea></section><aside class="review-panel"><h3>Verification checklist</h3><div class="review-list">${labels.map(([k,v])=>`<label><input type="checkbox" data-check="${k}" data-id="${x.id}" ${x.checks[k]?'checked':''}>${v}</label>`).join('')}</div><label class="stack-label">ViewYourSite address<input class="text-input" data-slug="${x.id}" value="${SitePipeline.escapeHTML(x.slug)}"></label><div class="review-actions"><button class="btn secondary" data-return="${x.id}">Return to Staging</button><button class="btn" data-live="${x.id}" ${labels.every(([k])=>x.checks[k])?'':'disabled'}>Send to Live</button></div></aside></article>`).join('')||'<div class="empty"><div><i class="bi bi-code-square"></i><strong>No sites under review.</strong></div></div>'}
 async function zip(x){const z=new JSZip();for(const f of x.files)z.file(f.path,await SitePipeline.download(x.id,f.path));const blob=await z.generateAsync({type:'blob'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${x.company.replace(/\W+/g,'-')}.zip`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
 root.addEventListener('change',async e=>{const x=items.find(i=>i.id===(e.target.dataset.id||e.target.dataset.slug));if(!x)return;if(e.target.dataset.check){x.checks[e.target.dataset.check]=e.target.checked;await SitePipeline.update(x.id,{checks:x.checks})}if(e.target.dataset.slug)await SitePipeline.update(x.id,{slug:e.target.value});load()});root.addEventListener('input',e=>{const x=items.find(i=>i.id===e.target.dataset.notes);if(x)x.notes=e.target.value});root.addEventListener('focusout',async e=>{const x=items.find(i=>i.id===e.target.dataset.notes);if(x)await SitePipeline.update(x.id,{notes:x.notes})});root.addEventListener('click',async e=>{const b=e.target.closest('button');if(!b)return;try{if(b.dataset.download)await zip(items.find(i=>i.id===b.dataset.download));if(b.dataset.return){const note=prompt('Required: explain what must be changed before this returns to Staging.');if(!note?.trim())return msg('A return note is required.',true);await SitePipeline.update(b.dataset.return,{status:'staging',returnNote:note.trim(),checks:{responsive:false,links:false,content:false,console:false}})}if(b.dataset.live)await SitePipeline.update(b.dataset.live,{status:'live',liveAt:new Date().toISOString()});await load()}catch(err){msg(err.message,true)}});load();
})();
